# Ingredients

Currently text-only (`src/data/ingredients.ts`) — no image slot wired up
yet. If you want a visual per ingredient, add an `image` field to each
`Ingredient` entry and render it with `<MediaFrame>` in
`IngredientSection.tsx`, the same pattern used everywhere else.
