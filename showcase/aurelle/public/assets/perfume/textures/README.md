# Textures

Reserved for optional overlay textures (subtle paper grain, fabric,
botanical scans) if you want to layer something behind sections instead of
a flat colour. Nothing currently references this folder.
