# Story / signature-sequence photography

Referenced by `src/data/productScene.ts` and `src/data/scentStory.ts`:

- `scene-01.jpg` … `scene-06.jpg` — the six beats of the signature pinned
  sequence (presence → detail → origin → wear → return → collection).
  Full-bleed, ~2400×1500px. A short muted `.mp4` per beat also works — see
  `public/assets/perfume/video/README.md` for scroll-scrubbed video wiring.
- `scent-story.jpg` — the "before the bottle" section image.
- `visual-break.jpg` — the full-width break ("Leave something behind").
- `brand-story.jpg` — the "About" section image.
- `final-scene.jpg` — the closing cinematic scene, before the MusfirahLoom
  showcase transition.
