# Product photography

One image per product, referenced by `src/data/coffeeProducts.ts`:

- `house-espresso.jpg`
- `ethiopian-light.jpg`
- `colombian-reserve.jpg`

Portrait orientation, ~1600×2000px, consistent lighting/background across
the set so the collection grid reads as one shoot. Adding a fourth product?
Add its image here, then add one entry to `COFFEE_PRODUCTS` — no component
changes needed.
